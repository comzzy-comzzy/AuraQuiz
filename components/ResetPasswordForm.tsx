"use client";
import {FormEvent,useEffect,useState} from "react";import Link from "next/link";import {createClient} from "@/lib/supabase/client";
import {PasswordInput} from "@/components/PasswordInput";
export function ResetPasswordForm(){const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[ready,setReady]=useState<boolean|null>(null);const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");useEffect(()=>{createClient().auth.getSession().then(({data})=>setReady(Boolean(data.session)))},[]);async function submit(e:FormEvent){e.preventDefault();setMessage("");if(password!==confirm){setMessage("The passwords do not match. Please enter them again.");return}setLoading(true);try{const{error}=await createClient().auth.updateUser({password});if(error)throw error;setMessage("Your password has been updated. Taking you back to your dashboard…");setTimeout(()=>location.href="/dashboard",1200)}catch(err){setMessage(err instanceof Error?err.message:"We could not update your password. Request a new recovery link.")}finally{setLoading(false)}}if(ready===null)return <p>Checking your recovery link…</p>;if(!ready)return <div className="recovery-invalid"><p>This recovery session is missing or has expired.</p><Link href="/forgot-password">Request a new link</Link></div>;return <form className="auth-form" onSubmit={submit}><label>New password<PasswordInput required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" autoComplete="new-password"/></label><label>Re-enter new password<PasswordInput required minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Enter the same password again" autoComplete="new-password"/></label><button className="primary full" disabled={loading}>{loading?"Updating…":"Set new password"}</button>{message&&<p className="form-message">{message}</p>}</form>}
