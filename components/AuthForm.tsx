"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {createClient} from "@/lib/supabase/client";
import {PasswordInput} from "@/components/PasswordInput";
export function AuthForm({mode}:{mode:"login"|"signup"}){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [confirmPassword,setConfirmPassword]=useState("");const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");
async function submit(e:FormEvent){e.preventDefault();setMessage("");if(mode==="signup"&&password!==confirmPassword){setMessage("The passwords do not match. Please enter them again.");return}setLoading(true);try{const s=createClient();if(mode==="signup"){const {error}=await s.auth.signUp({email,password});if(error)throw error;location.href="/dashboard";return}else{const {error}=await s.auth.signInWithPassword({email,password});if(error){setMessage("We couldn’t sign you in. Check your email and password. If you’re new to Aura Academy, please create an account first.");return}location.href="/dashboard";} }catch(err){setMessage(err instanceof Error?err.message:"Something went wrong. Please try again.");}finally{setLoading(false)}}
return <form onSubmit={submit} className="auth-form">{mode==="login"&&<div className="new-user-note"><b>New to Aura Academy?</b><span>You need an account before you can sign in.</span><Link href="/signup">Create an account first</Link></div>}<label>Email address<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></label><label>Password<PasswordInput required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" autoComplete={mode==="login"?"current-password":"new-password"}/></label>{mode==="signup"&&<label>Re-enter password<PasswordInput required minLength={8} value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Enter the same password again" autoComplete="new-password"/></label>}{mode==="login"&&<div className="forgot-link"><Link href="/forgot-password">Forgot your password?</Link></div>}<button className="primary full" disabled={loading}>{loading?"Please wait…":mode==="login"?"Sign in to learn":"Create free account"}</button>{message&&<p className="form-message">{message}</p>}<p className="auth-switch">{mode==="login"?"New to Aura Academy?":"Already have an account?"} <Link href={mode==="login"?"/signup":"/login"}>{mode==="login"?"Create an account":"Sign in"}</Link></p></form>}
